{
  "name": "heat-backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "vitest": "^4.0.16",
    "wrangler": "^4.4.0"
  }
}